server:
  port: 8083

spring:
  application:
    name: notification-service

  kafka:
    bootstrap-servers: kafka:29092
    consumer:
      group-id: notification-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      # Jackson 3 deserializer (Boot 4 default). Replaces the deprecated JsonDeserializer.
      value-deserializer: org.springframework.kafka.support.serializer.JacksonJsonDeserializer
      properties:
        spring.json.trusted.packages: "com.sanchit.payment_service.dto,com.sanchit.notification_service.dto"
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.sanchit.notification_service.dto.PaymentEvent

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  prometheus:
    metrics:
      export:
        enabled: true

kafka:
  topics:
    payment-processed: payment.processed
